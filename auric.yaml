# This is the Auric configuration file.

roms:
  # Path to the ROMS directory.
  roms_directory: ./ROMS

  # Names of the ROM files.
  file_names:
    oric_1: basic10.rom
    oric_atmos: basic11b.rom
    microdisk: microdis.rom

media:
  # Path to the images directory.
  images_path: ./images
  # Path to the fonts directory.
  fonts_path: ./fonts

video:
  # Controls the window width and height
  window_width: 1024
  window_height: 768

  # Enable/disable CRT scan line artifacts.
  enable_scanlines: true

  # Enable/disable CRT vertical line artifacts.
  enable_vertical_lines: true

  # Enable/disable CRT vignette.
  enable_vignette: true

  # Controls CRT vignette strength (0 - 1).
  vignette_strength: 0.2

tape:
  # Enables tape turbo load (bypasses ROM loading routines for faster loading).
  turbo_load_enabled: true

  # Enables automatic tape loading at start-up (echoes CLOAD command).
  autostart_enabled: true